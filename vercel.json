{
  "rewrites": [
    {
      "source": "/ocr/(.*)",
      "destination": "${process.env.REACT_APP_OCR_API_INVOKE}/$1"
    },
    {
      "source": "/api/(.*)",
      "destination": "${process.env.REACT_APP_GPT_API_INVOKE}/$1"
    }
  ]
}
