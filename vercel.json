{
  "rewrites": [
    {
      "source": "/ocr/(.*)",
      "destination": "${env.REACT_APP_OCR_API_INVOKE}/$1"
    },
    {
      "source": "/api/(.*)",
      "destination": "${env.REACT_APP_GPT_API_INVOKE}/$1"
    }
  ]
}
